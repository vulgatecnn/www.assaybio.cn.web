server {
    listen 80;
    server_name assaybio.cn www.assaybio.cn;
    root /var/www/html/assaybio;
    index index.html index.htm;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-XSS-Protection "1; mode=block" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header Referrer-Policy "no-referrer-when-downgrade" always;

    # 主页面路由
    location / {
        try_files $uri $uri/ $uri.html /index.html;
    }

    # 静态资源缓存优化
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        expires 1y;
        add_header Cache-Control "public, immutable";
        access_log off;
        
        # 添加CORS头（如果需要）
        add_header Access-Control-Allow-Origin "*";
    }

    # HTML文件缓存
    location ~* \.html$ {
        expires 1h;
        add_header Cache-Control "public, must-revalidate";
    }

    # 上传目录
    location /Upload/ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # 图片目录
    location /images/ {
        expires 30d;
        add_header Cache-Control "public";
    }

    # JS目录
    location /js/ {
        expires 1y;
        add_header Cache-Control "public, immutable";
    }

    # 隐藏敏感文件
    location ~ /\. {
        deny all;
        access_log off;
        log_not_found off;
    }

    location ~ /\.git {
        deny all;
        access_log off;
        log_not_found off;
    }

    # 错误页面
    error_page 404 /404.html;
    error_page 500 502 503 504 /50x.html;

    # 日志
    access_log /var/log/nginx/assaybio.access.log;
    error_log /var/log/nginx/assaybio.error.log;
}

# HTTPS配置（SSL证书配置后启用）
# server {
#     listen 443 ssl http2;
#     server_name assaybio.cn www.assaybio.cn;
#     root /var/www/html/assaybio;
#     index index.html index.htm;
#
#     ssl_certificate /etc/ssl/certs/assaybio.crt;
#     ssl_certificate_key /etc/ssl/private/assaybio.key;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers ECDHE+AESGCM:ECDHE+CHACHA20:DHE+AESGCM:DHE+CHACHA20:!aNULL:!SHA1:!WEAK;
#     ssl_prefer_server_ciphers on;
#
#     # 其他配置与HTTP相同...
# }

# HTTP到HTTPS重定向（SSL证书配置后启用）
# server {
#     listen 80;
#     server_name assaybio.cn www.assaybio.cn;
#     return 301 https://$server_name$request_uri;
# }